DATABASE_URL="file:./dev.db"

# Academic import authorization key.
# Set this to a strong random secret in production.
# The POST /api/academic/import endpoint will require this as the X-API-Key header.
# Leave unset (or empty) to disable auth check in development.
IMPORT_API_KEY="your-secret-key-here"
